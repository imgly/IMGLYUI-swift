/// A namespace for all ``PostcardEditor`` related classes.
public enum Postcard {
  /// Icons used for the ``PostcardEditor``.
  public enum Icon {}

  /// Sheet types used for the ``PostcardEditor``.
  public enum SheetType {}

  /// A namespace for the default implementations of `OnChanged`.
  enum OnChanged {}
}
